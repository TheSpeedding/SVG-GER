PROGRAM Triangles {
  SET_COLOR(0, 255, 0).
  DRAW_TRIANGLE(0, 0).
  DRAW_TRIANGLE(50, 0).
  DRAW_TRIANGLE(100, 0).
}
FUNCTION DRAW_TRIANGLE(x, y) { // x, y represent basepoint for the triangle
  MOVE_TO(x, y+50).
  PEN_DOWN.
  MOVE_TO(x+25, y).
  MOVE_TO(x+50, y+50).
  MOVE_TO(x, y+50).
  PEN_UP.
  MOVE_TO(x, y).
}